<?xml version="1.0" encoding="utf-8" ?>
<base:EduTaskPage xmlns:base="clr-namespace:EDUTASK_1._1.Views.Base"
                  xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                  xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                  x:Class="EDUTASK_1._1.Views.CompletedTasksPage"
                  NavigationPage.HasNavigationBar="False"
                  Shell.NavBarIsVisible="False"
                  BackgroundColor="{StaticResource SurfaceMuted}">

    <Grid RowDefinitions="Auto,*">

        <Grid Style="{StaticResource AppBar}">
            <Label Text="Completed Tasks"
                   Style="{StaticResource PageTitleInverse}"
                   SemanticProperties.HeadingLevel="Level1"/>
        </Grid>

        <VerticalStackLayout Grid.Row="1"
                             Spacing="10"
                             Padding="32,0"
                             MaximumWidthRequest="360"
                             HorizontalOptions="Center"
                             VerticalOptions="Center">

            <Border WidthRequest="72" HeightRequest="72"
                    BackgroundColor="{StaticResource StatusSuccessSurface}"
                    StrokeThickness="0"
                    HorizontalOptions="Center"
                    Margin="0,0,0,6">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="36"/>
                </Border.StrokeShape>
                <Label Text="✓"
                       FontSize="32"
                       FontAttributes="Bold"
                       TextColor="{StaticResource StatusSuccess}"
                       HorizontalTextAlignment="Center"
                       VerticalTextAlignment="Center"/>
            </Border>

            <Label Text="Nothing completed yet"
                   Style="{StaticResource EmptyStateTitle}"/>

            <Label Text="Tasks you and your teachers finish will be archived here."
                   Style="{StaticResource EmptyStateMessage}"/>
        </VerticalStackLayout>
    </Grid>
</base:EduTaskPage>
